dependencies:
  idf:
    version: ">=5.3,<6.0"   # PINNED: ver docs/HARDWARE.md (chip ES rev v1.3)

  # Driver do painel ST7701S via MIPI-DSI (série 1.x p/ IDF 5.x).
  espressif/esp_lcd_st7701:
    version: "^1.1.5"

  # Driver do touch GT911 (I2C).
  espressif/esp_lcd_touch_gt911:
    version: "^1.1.3"

  # Driver USB Host HID (teclado).
  espressif/usb_host_hid:
    version: "^1.0.0"

  # UI toolkit (mesma versão validada no protótipo).
  slint/slint:
    version: "^1.12.1"

  # Sistema de arquivos interno.
  joltwallet/littlefs:
    version: "^1.14.8"

  # Lua 5.5 oficial da Espressif (scripts + config do sistema).
  espressif/lua:
    version: "^5.5.0"
